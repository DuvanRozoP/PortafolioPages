import { AnimationsNavarDirective } from './animations-navar.directive';

describe('AnimationsNavarDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationsNavarDirective();
    expect(directive).toBeTruthy();
  });
});
