import { AnimationsRelaxDirective } from './animations-relax.directive';

describe('AnimationsRelaxDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationsRelaxDirective();
    expect(directive).toBeTruthy();
  });
});
